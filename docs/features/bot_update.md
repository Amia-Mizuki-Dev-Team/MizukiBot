---
title: Amia_晓山瑞希更新日志
description: Amia_晓山瑞希的功能新增、兼容性修改、问题修复和历史版本记录。
pageClass: amia-update-log
---

# Amia_晓山瑞希更新日志

本页只记录 Bot 本身的指令、插件、兼容性和问题修复。服务器维护、机房迁移、团队维护安排及 HongXing 关联产品调整统一记录在[服务状态与公告](/service-announcements)。

## 2026 年 7 月

### PJSK 功能与底层稳定性

- 将客户端代理引擎从 `websockets` 升级为 `aiohttp`，缓解 Windows 和双栈网络环境下频繁出现的远程连接拒绝问题；
- 补全 NapCat / OneBot V11 握手请求头，并处理登录信息与生命周期 API 回调，降低被后端防火墙误判为非法客户端的概率；
- 修复存活心跳包处理逻辑错误导致的代理与客户端循环断线重连；
- 重构双端指令池，将指令划分为专有指令与共用指令，修复“补票”“倍率”等特殊指令及工具箱指令被代理误拦截的问题；
- 优化外链清洗规则，正确放行 `[CQ:image,file=...]` 等内部图片链接，避免误删游戏截图与资源；
- 新增指定群聊免斜杠触发，并使用 `auto_slash_groups.json` 保存启用状态；
- 免斜杠配置仅限 Bot 超级管理员通过 `免斜杠配置 群号` 或 `免斜杠取消 群号` 热更新，并在帮助菜单中增加风险提示；
- 重构帮助菜单，扩充帮助按钮，将笼统描述替换为可直接复制的指令示例；
- 修复帮助菜单中工具箱地址无法点击的问题，适配标准 Markdown 超链接格式。

### 经济榜与经济系统

#### 图像渲染

- 修复打工结算图片中收益正负号拼接错误，负收益不再显示为 `+-2 PC`。

#### 数据库与业务逻辑

- 修复排行榜 SQL 变量转义崩溃，将 `DATE_FORMAT` 中的百分号正确转义，避免 `aiomysql` 格式化变量误判；
- 为发红包、抢红包和商城购买事务补充 `economy_logs` 账单流水；
- 为红包账单加入 6 位字母数字混合序列号，便于查账、防重和溯源；
- 适配网页排行榜字段并自动计算排名序号。

#### 接口与交互

- 将 `safe_execute` 并发超时时间从 10 秒调整为 45 秒，降低高并发或图片生成时的误报警概率；
- 通过 FastAPI 挂载 `/economy/rank` GET 接口并配置 CORS，使 `list.mizuki.top` 可以读取实时排行榜；
- 新增商城提货指令 `结算订单`，别名为 `购买`，用于承接网页商城生成的文本命令；
- 增强 PK 目标识别，支持回复对象和消息内 at 节点，并过滤无效目标；
- 区分 QQ 官方 Bot 的 Markdown 与按钮流程和普通 Bot 的纯文本降级流程；
- 修复 `经济帮助` 菜单硬编码玩家昵称的问题，改为动态读取当前触发者昵称。

### 白名单与群聊鉴权

- 将授权成功缓存设为 10 分钟，失败状态缓存缩短为 30 秒，减少群管授权后的恢复等待时间；
- 改为遍历群成员列表提取 `role == "owner"` 的群主身份，并自动修复旧数据中的无效群主记录；
- 增加全局拦截钩子，阻断未授权群聊消息进入其他插件；
- 拦截提示仅向群主或管理员返回，并设置 5 分钟冷却；
- 管理面板弃用旧版 Tortoise ORM 查询方式，改用兼容 `nonebot-plugin-orm` 的异步 SQLAlchemy 查询；
- 管理面板使用树状明细展示群名、群主和邀请人等信息，优化移动端可读性；
- 增加状态标识，区分超级管理员特批与开发群授权放行。

## 2026 年 4 月

### 核心与安全

- 更新文本审核和上下文检索流程，并完善阶梯处理机制；
- Bot 鉴权防护框架接入 HongXing AuthLit 4；
- 底层预处理流程接入 HongXing OpenAis；
- 优化消息回复间隔和引用原消息的交互方式。

### 经济系统与舞萌 DX

- 更新打工界面，修复连续打工触发异常；
- 经济榜改为每 3 分钟更新一次；
- 在舞萌 DX 查询中增加 AP50，并规划热力图、分数趋势和 FC50 等能力；
- Mai-Sync 增加水鱼与落雪双端上传、收藏品自动同步、账号预览、强制停止、取消绑定和队列处理；
- Mizuki-Sync 第一版进入规划，原计划于 2026 年 5 月或 6 月开展首轮内测。

## 2026 年 3 月 1 日

### 经济系统进阶与修复

- 新增 `经济帮助` 文字说明；
- 增加 CDK 兑换流程，用户可在 `store.mizuki.top` 复制兑换文案并发送给 Bot；
- 体力不足时显示下一次恢复时间；
- 修复打工随机分配问题；
- 修复个人信息与经济榜等级显示不一致的问题；
- 修复经济榜与商城的若干问题。

## 2026 年 2 月 20 日

### 新年版本

- 新增商店与背包，可购买补给、恢复、头衔和主题等虚拟内容，并保存至背包；
- 修复 `list.mizuki.top` 排行榜页面缩放和注册时间显示问题；
- 更新黄历事件、签到语录和相关文案；
- 更新欢迎与离群提示；
- 更新 Bilibili 动态订阅，支持多群订阅；
- 修复猜卡面和猜歌上传较大文件失败的问题；
- 移除使用率较低的 AI VITS 语音服务。

## 2026 年 2 月 16 日

### 服务分流与功能重构

当时 Bot 服务被划分为综合功能实例与 PJSK 专属实例：

- 综合功能实例：1 号机 `3986839028`、3 号机 `3947949425`、5 号机 `3904382316`；
- PJSK 专属实例：2 号机 `1005524479`、4 号机 `3925294264`、6 号机 `3675727244`。

功能调整包括：

- 重构签到、个人信息、经济帮助、转账和 PK 流程；
- 更新黄历、随机事件、熟练度、打工类型、商城、用户等级、称号和主题色；
- 清理旧版状态、签到、欢迎语言和语音服务实现。

## 2026 年 2 月 6 日至 13 日

- 上线个人信息、签到、PK、熟练度、转账、红包和每日随机事件；
- 拉取 Bot 进入群聊需先进入审核群 `1053964431`；
- 增加 PJSK 使用次数限制和多 Bot 同群分流响应机制；
- 接入烤森功能；
- 当时签到奖励范围为 5 至 30 PC，转赠手续费为 4，单日限制为 5 次。

## 2026 年 2 月 3 日

- 更新水鱼与落雪成绩上传；
- 新增玩家成就查询；
- 更新落雪 OAuth 绑定流程；
- 更新舞萌成绩同步免责声明；
- 完善业余无线电日志的中继查询、ADIF 导出和呼号绑定及解绑功能。

## 2026 年 1 月 27 日至 31 日

- 将旧 Feedback 插件替换为飞书或腾讯文档等外部反馈表单；
- 实现舞萌成绩上传，并允许部分插件按群聊关闭；
- 新增项目联系邮箱 `Amia@mizuki.top`；
- 开始重构公告和成绩同步相关能力。

## 2026 年 1 月 25 日

### PJSK 车队助手

- 支持“5 位房号 + 属性 + 缺人数”的发车格式，并支持“战力”“普通”“时效”等属性；
- 发送“满”可标记满员，重新发送可补人，新房号会替换旧房号；
- 支持多群共同使用车队；
- 群管可申请车队广播，经超级管理员审核后向相关群聊发布。

## 2026 年 1 月 19 日至 24 日

- 暂时移除不稳定的 AI 功能；
- 重写业余无线电模块 `ham_qso`；
- 关闭 Bot 私聊功能，统一在群聊场景中使用。

## 2026 年 1 月 1 日至 11 日

- 发布正式版本，集成 PJSK、舞萌 DX 和群聊娱乐模块；
- 重构 Meme 系统并接入海豚数据库；
- 增加 Meme 按群聊屏蔽能力；
- 启用 4 号机作为备用 Bot，仅在主 Bot 受限或测试期间接替服务；
- 历史技术支持鸣谢：Te:Rivet、uni_mayu、ecomter、Sirius、zhu，排名不分先后。

历史记录只反映当时版本状态、账号分配和规则，不代表相关功能目前仍然开放。当前使用方式以功能文档和 Bot 内帮助菜单为准。
