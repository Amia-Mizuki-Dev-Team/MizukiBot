---
title: 功能状态与数据来源
description: 说明 Amia_晓山瑞希各功能模块的公开状态、数据来源、更新方式、依赖关系与已知限制，并提供服务器监控入口与 HongXing 产品生命周期。
aside: false
---

# 功能状态与数据来源

本页用于解释各模块“依赖什么、何时更新、出现异常时应检查哪里”。插件正在更新、测试或分批部署时，Bot 实际返回和 Bot 内帮助菜单优先于本文。

<div class="mzk-card mzk-card--blue">
  <h3>实时服务器监控</h3>
  <p>服务器在线状态、运行时间和资源指标以 Komari 监控面板为准。监控面板与本文的功能状态说明相互独立。</p>
  <p><a href="https://komari.mizuki.top" target="_blank" rel="noopener noreferrer"><strong>打开 Komari 服务器监控</strong></a></p>
</div>

<div class="mzk-card mzk-card--orange">
  <h3>Release010 当前状态</h3>
  <p>Release010 已完成 15 个生态仓库的代码与 README 发布，194 项自动化测试全部通过。</p>
  <ul>
    <li>代码与 README：已发布</li>
    <li>自动化测试：<code>194/194 PASS</code></li>
    <li>正式仓库推送：<code>15/15</code> 完成</li>
    <li>真实 QQ / Gensokyo 手动验证：待完成</li>
    <li>生产启用：待批准</li>
  </ul>
  <p>完整变更见 <a href="/features/bot_update#2026-年-8-月release010"><strong>Release010 更新说明</strong></a>。</p>
</div>

维护、迁移和计划停机仍以[服务状态与公告](/service-announcements)为准。监控面板显示在线，也不代表所有插件和第三方数据源均可正常使用。

## HongXing 产品与服务生命周期

以下时间轴展示 HongXing Online、HongXingOS、HongXing Firmware、HongXing AuthLit 以及相关实验服务的公开生命周期信息。HongXing Online 3 当前没有规划，因此不纳入时间轴；未知的未来生命周期阶段不会推测填写。

<HongXingLifecycle />

## 状态说明

- **已上线**：已经在正式 Bot 或公开服务中使用；
- **Release010 候选**：代码、文档和自动化测试已完成，但真实 QQ / Gensokyo 验证或生产批准尚未完成；
- **依赖上游**：模块本身已提供，但可用性受第三方接口、游戏数据或远程服务影响；
- **按需启用**：不会默认在所有群开启，需要管理员或用户主动配置；
- **测试中**：已有实现，仍在进行兼容性或生产验证；
- **独立服务**：与 Bot 主进程分开运行，维护安排可能不同。

“已上线”不代表永久无故障，也不构成持续可用性承诺。标记为 Release010 候选的能力，在生产批准前不应视为已正式启用。

## 用户功能状态

<div class="mzk-table-wrapper">

| 模块 | 公开状态 | 主要数据来源 | 更新方式 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| PJSK 基础资料与歌曲查询 | 已上线；Release010 兼容更新待启用 / 依赖上游 | PJSKHelp、本地帮助、Haruki 与 Sakura 网关 | 按请求获取 | Release010 增强消息兼容和帮助聚合；区服支持范围仍以 Bot 返回为准。 |
| PJSK 绑定与个人资料 | 已上线 / 依赖上游 | 用户绑定关系、验证状态和账号公开数据 | 用户触发查询 | 他人查询受隐私设置限制；绑定后默认不开放他人查询。 |
| PJSK 榜线、Rating 与组卡 | 已上线 / 依赖上游 | 活动数据、suite 上传数据和后端计算结果 | 按请求获取或根据上传数据计算 | 数据未上传、过期或区服不支持时，进阶结果可能不可用。 |
| PJSK MySekai | 依赖上游 | 用户上传的 MySekai 数据与对应后端 | 用户上传后查询 | JP 支持范围相对完整，其他区服以实际返回为准。 |
| PJSK 协力车队 | 按部署启用 | 群聊事件、房间信息与跨群广播状态 | 实时事件处理 | 是否开放及广播范围由当前部署和群配置决定。 |
| 舞萌 DX 查分与分析 | 已上线；Release010 更新待启用 / 依赖上游 | LXNS、DivingFish 与插件本地计算 | 按请求获取 | Release010 优化 B50、曲库、进度、个人状态、错误码和脱敏诊断，并同步 CN1.56 数字资源。 |
| 舞萌 DX 成绩同步 | 独立服务 / 定时维护 | 水鱼、落雪授权信息和同步队列 | 用户触发同步 | 每日 00:00—11:00 暂停服务；队列最多 10 人，同时处理 1 人。 |
| 舞萌服务器状态 | 已上线 / 社区数据 | 用户上报、群聊触发与状态汇总 | 实时或人工上报 | 上报内容反映社区观测，不等同于官方故障公告。 |
| Chunithm 国服查询 | Release010 候选 / 依赖上游 | DivingFish 国服数据 | 按请求获取 | 候选指令包括 `chusearch`、`chuinfo`、`chuchart` 和 `chu b30`；当前仅支持国服。 |
| 经济系统 | 已上线；Release010 快捷入口待启用 | Bot 内部持久化数据与业务规则 | 实时写入 | Release010 在打工和排行榜结果中增加安全快捷入口，不提供直接改变资产的高风险快捷操作。 |
| 帮助菜单与功能索引 | 已上线；Release010 帮助系统待启用 | Amia 入口、PicMenu Next 与插件注册信息 | 随部署更新 | 候选版本支持图片、分类、详情、模糊和拼音搜索，并可在 Markdown 或图片不可用时降级为中文文本。 |
| 入群欢迎与离群提示 | 已上线；Release010 更新待启用 | QQ 群成员事件、独立图库与群配置 | 事件触发 | 候选版本加入随机中文文案、正确 @、静态图与 GIF 组合，并修复成员头像误识别。 |
| 个人档案 | Release010 候选 | QBind、活跃统计、经济、Meme 与 PJSK Provider | 用户触发聚合 | 单个数据模块缺失、超时或失败时，不阻断整张个人档案生成。 |
| Meme 表情包与图片管理 | 已上线；Release010 菜单待启用 | 本地图库、分类配置与用户上传内容 | 用户触发 | 普通用户菜单改为中文；删除、溯源、锁定和屏蔽群等管理能力继续保留但不公开展示。 |
| 群公告与自动分发 | 按需启用 | 管理员配置、群事件和本地记录 | 事件触发 | 该模块主要负责群公告管理和自动补发，不是完整群管理系统。 |
| 基础群活统计 | 已上线 | 匿名或聚合后的消息活动记录 | 持续统计 | 用于今日发言、本月发言、全群排行和 DAU 等基础指标。 |
| 群聊活跃分析 | 测试中 / 按需启用 | 启用后的匿名活动元数据 | 管理员开启后持续记录 | 默认不采集所有群，也不会补算开启前的数据。 |
| Minecraft 服务器 | 独立服务 | 服务器实例、游戏端和监控信息 | 独立维护 | 地址、版本和维护安排以群公告及对应更新页面为准。 |
| Komari 服务器监控 | 独立服务 | Komari Agent 与监控面板 | 持续采集 | 展示服务器在线状态与资源指标，不判断单个插件或第三方接口是否可用。 |
| 帮助文档站 | 已上线 | MizukiBot 仓库中的 Markdown 文档 | 提交后自动构建部署 | 文档与插件生产部署可能存在短时间差异。 |

</div>

## Release010 身份与诊断边界

Release010 继续使用字符串与 OpenID 身份体系。平台事件中的群会话 ID 不一定等于 QQ 客户端显示的群号，两者不会被视为同一概念。

候选版本的错误提示统一包含错误码、简短原因、下一步建议和可选诊断附件。诊断内容不会包含 Token、数据库连接、完整 OpenID、本地绝对路径或完整异常栈。

生产启用前仍需要完成真实 QQ / Gensokyo 环境验证，包括消息段、Markdown、Keyboard、图片、@成员、身份解析和错误附件发送。

## 数据边界与隐私

### 账号绑定数据

绑定关系只用于识别用户选择的游戏账号和默认查询对象。公开查询能力受具体模块、区服和用户隐私设置限制。引继码、密码、Token、OAuth 授权码及登录二维码不得提交到公开群聊或反馈表单。

### PJSK 上传数据

部分 Rating、组卡、详细资料和 MySekai 功能依赖用户主动上传的数据。上传数据是否有效、更新时间和支持范围由对应后端决定。Bot 无法替用户找回游戏账号。

### 舞萌 DX 数据

查询插件和成绩同步组件相互独立：

- 查询与分析模块读取已存在的查分数据并生成结果；
- 同步组件负责用户授权、排队、上传和会话清理；
- 一侧异常不一定代表另一侧同时异常。

### 群聊统计数据

基础发言统计与群聊活跃分析不是同一个模块。群聊活跃分析默认关闭，只有管理员在目标群主动开启后才开始记录匿名活动元数据。

公开的数据边界包括：

- 不保存消息正文；
- 不保存昵称；
- 不保存真实 QQ 号；
- 不保存平台虚拟用户 ID 或群 ID 明文；
- 不采集图片、语音和文件内容；
- 不采集私聊内容。

## 数据更新与延迟

不同模块的“更新”含义不同：

- **实时事件**：群消息、成员变动和部分群聊统计在事件到达后处理；
- **按请求查询**：歌曲、资料、榜线和成绩在用户发送指令时获取；
- **上传后更新**：suite、MySekai 和舞萌成绩需要用户先完成上传或同步；
- **队列处理**：舞萌成绩同步受维护窗口和排队人数影响；
- **持续监控**：Komari Agent 定期上报服务器运行指标；
- **候选发布**：代码和测试已完成，但尚未完成生产启用；
- **部署更新**：插件代码与帮助文档需要经过测试和生产部署，不保证与仓库提交同时生效。

上游服务可能存在缓存、限流、维护或数据延迟，因此“游戏内已经变化”不等于 Bot 会立即显示新结果。

## 出现异常时先检查什么

<div class="mzk-table-wrapper">

| 现象 | 优先检查 |
| :--- | :--- |
| 监控面板显示服务器离线 | 主机、电源、网络、Agent 与监控面板连接 |
| 监控面板在线但 Bot 无响应 | Bot 进程、群消息权限、平台链路和插件日志 |
| Release010 文档已有但功能未出现 | 生产启用是否已批准，正式 Bot 是否已经部署候选版本 |
| 所有指令都无响应 | Bot 在线状态、群消息权限和平台链路 |
| 只有 PJSK 模块失败 | 区服、绑定、验证、上传数据和 PJSK 上游 |
| 只有舞萌查分失败 | 数据源绑定、默认路由和查分器状态 |
| 只有舞萌同步失败 | 维护窗口、队列、OAuth / Token 和异常会话 |
| 只有图片无法发送 | 图片生成、上传链路和 QQ 平台限制 |
| 群活数字不一致 | 统计模块、启用时间和统计口径 |
| 管理指令提示无权限 | 群角色、超级用户、身份映射和权限服务 |
| 文档与实际不一致 | Bot 内帮助、更新日志和当前生产部署 |

</div>

详细排查步骤见[常见问题与故障排查](/faq)。确认问题稳定存在后，再按[问题反馈](/feedback)要求提交完整信息。

## 面向开发者的状态

用户功能状态与仓库开发进度不是同一概念。插件可能已经有实现但仍待生产验证，也可能正在进行兼容更新而不影响现网旧版本。

公开项目职责与组件关系见[项目生态](/projects/)。仓库认领、测试状态和协作边界见[开发者招募与协作](/recruiting)。未明确标记为稳定发布的能力，不应视为正式服务承诺。