name: Refresh Wrangler lockfile

on:
  push:
    branches:
      - maintenance/wrangler-lock-20260801
    paths:
      - package.json
      - package-lock.json
      - .npmrc
      - .github/workflows/refresh-wrangler-lock.yml

permissions:
  contents: write

jobs:
  refresh-lockfile:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout maintenance branch
        uses: actions/checkout@v6
        with:
          ref: maintenance/wrangler-lock-20260801

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: npm

      - name: Regenerate package lock
        run: npm install --package-lock-only

      - name: Commit lockfile update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json

          if git diff --cached --quiet; then
            echo "package-lock.json already matches package.json"
            exit 0
          fi

          git commit -m "chore: lock Wrangler dependencies"
          git push origin HEAD:maintenance/wrangler-lock-20260801
